**/venv/** 
venv

*.pyc
**__pycache__/
*.pyo
*.pyd



*.pdb
**debug**
cache/**

grid**
profiles**
*.cif
*/**.pyc


**/q_*.md
q_*.md

**env
.env


*fasta
# ...but the per-family master alignments ARE runtime-critical: the ETL
# variants/isotype steps read data/<family>_tubulin/*.afasta. Without these
# in the image, collection fails for every non-alpha chain. (alpha's afasta
# predates the *fasta rule, which is why only it survived.)
!data/*_tubulin/*.afasta

*.gz

notes
notes/**

**/.DS_Store


TUBETL_DATA/**
TUBETL_DATA

# api/query_log.py falls back to ./query_log.jsonl when TUBETL_DATA is unset,
# so local runs drop it in the repo root.
query_log.jsonl

ingestion_logs/*


node_modules/**
node_modules


*pkl

*.xlsx
.claude/
